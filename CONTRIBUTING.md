# Руководство по внесению вклада

Спасибо за интерес к проекту Telegram Video Bot! Мы приветствуем любые улучшения и исправления.

## Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, что ошибка еще не была зарегистрирована в Issues
2. Создайте новый Issue с подробным описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Версия Python, ОС
   - Логи (если применимо)

### Предложение новых функций

1. Создайте Issue с описанием функции
2. Объясните, зачем она нужна
3. Предложите возможную реализацию

### Pull Requests

1. **Fork репозитория**

```bash
git clone https://github.com/your-username/telegram-video-bot.git
cd telegram-video-bot
```

2. **Создайте ветку для изменений**

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

3. **Внесите изменения**

- Следуйте стилю кода проекта
- Добавьте комментарии к сложным участкам
- Обновите документацию при необходимости

4. **Проверьте код**

```bash
# Проверка синтаксиса
python -m py_compile *.py

# Форматирование (опционально)
black *.py

# Линтинг (опционально)
pylint *.py
```

5. **Commit и Push**

```bash
git add .
git commit -m "feat: добавлена новая функция X"
# или
git commit -m "fix: исправлена ошибка Y"

git push origin feature/your-feature-name
```

6. **Создайте Pull Request**

- Опишите изменения
- Укажите связанные Issues
- Добавьте скриншоты (если применимо)

## Стиль кода

### Python

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints где возможно
- Максимальная длина строки: 100 символов
- Используйте docstrings для функций и классов

Пример:

```python
async def process_video(file_path: str, user_id: int) -> dict | None:
    """
    Process video file and return results
    
    Args:
        file_path: Path to video file
        user_id: Telegram user ID
        
    Returns:
        Dictionary with results or None if failed
    """
    # Implementation
    pass
```

### Commit Messages

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование, отступы
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

Примеры:
```
feat: добавлена поддержка .mp4 файлов
fix: исправлена ошибка конвертации больших файлов
docs: обновлен README с примерами использования
```

## Структура проекта

```
telegram-video-bot/
├── main.py              # Точка входа
├── config.py            # Конфигурация
├── logger.py            # Логирование
├── handlers.py          # Telegram handlers
├── api_client.py        # API клиенты
├── utils.py             # Утилиты
├── requirements.txt     # Зависимости
├── Dockerfile           # Docker
└── tests/              # Тесты (планируется)
```

## Тестирование

В настоящее время проект не имеет автоматических тестов. Вклад в создание тестов приветствуется!

### Ручное тестирование

Перед отправкой PR убедитесь, что:

1. Бот запускается без ошибок
2. Основные функции работают:
   - Скачивание файлов
   - Конвертация видео
   - Транскрибация
   - Генерация саммари
3. Обработка ошибок работает корректно
4. Логирование информативно

## Зависимости

При добавлении новых зависимостей:

1. Добавьте в `requirements.txt` с указанием версии
2. Обновите `Dockerfile` если нужны системные пакеты
3. Опишите зависимость в документации

## Документация

При изменении функционала обновите:

- `README.md` - основная документация
- `DEPLOYMENT.md` - если изменения касаются развертывания
- Docstrings в коде
- Комментарии к сложным участкам

## Вопросы и помощь

Если у вас есть вопросы:

1. Проверьте существующие Issues и Discussions
2. Создайте новый Issue с тегом "question"
3. Опишите проблему максимально подробно

## Code Review

Все Pull Requests проходят review. Будьте готовы к:

- Вопросам о реализации
- Предложениям по улучшению
- Запросам на изменения

Это нормальный процесс, помогающий поддерживать качество кода!

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

---

Спасибо за ваш вклад! 🎉
