# Быстрый старт

Минимальная инструкция для запуска бота за 5 минут.

## Предварительные требования

Получите необходимые токены:

1. **Telegram Bot Token**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните полученный токен

2. **Fireworks API Key**
   - Зарегистрируйтесь на [Fireworks.ai](https://fireworks.ai/)
   - Перейдите в раздел API Keys
   - Создайте новый ключ
   - Сохраните ключ

3. **OpenAI API Key**
   - Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
   - Перейдите в API Keys
   - Создайте новый ключ
   - Сохраните ключ

## Вариант 1: Docker (рекомендуется)

```bash
# 1. Скачайте проект
git clone <repository-url>
cd telegram-video-bot

# 2. Создайте .env файл
cp .env.example .env

# 3. Отредактируйте .env и вставьте ваши токены
nano .env

# 4. Запустите
docker-compose up -d

# 5. Проверьте логи
docker-compose logs -f
```

## Вариант 2: Python

```bash
# 1. Скачайте проект
git clone <repository-url>
cd telegram-video-bot

# 2. Создайте .env файл
cp .env.example .env

# 3. Отредактируйте .env и вставьте ваши токены
nano .env

# 4. Запустите скрипт
./run.sh
```

## Вариант 3: Вручную

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Установите ffmpeg
sudo apt-get install ffmpeg  # Ubuntu/Debian
brew install ffmpeg          # macOS

# 3. Создайте .env
cp .env.example .env
nano .env  # Вставьте токены

# 4. Запустите
python main.py
```

## Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте `/start`
4. Отправьте видео или аудио файл
5. Дождитесь результата

## Пример .env файла

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
FIREWORKS_API_KEY=fw_1234567890abcdef
OPENAI_API_KEY=sk-1234567890abcdef
TARGET_THREAD_ID=
LOG_LEVEL=INFO
```

## Что дальше?

- Прочитайте [README.md](README.md) для подробной информации
- Изучите [DEPLOYMENT.md](DEPLOYMENT.md) для production развертывания
- Настройте бота под ваши нужды в `config.py`

## Проблемы?

- Проверьте логи: `docker-compose logs` или `python main.py`
- Убедитесь, что все токены корректны
- Проверьте, что ffmpeg установлен: `ffmpeg -version`
- Создайте Issue в репозитории

## Полезные команды

```bash
# Docker
docker-compose logs -f          # Просмотр логов
docker-compose restart          # Перезапуск
docker-compose down            # Остановка

# Python
python main.py                 # Запуск
Ctrl+C                        # Остановка
```

---

**Готово!** Ваш бот работает и готов обрабатывать видео встречи. 🎉
